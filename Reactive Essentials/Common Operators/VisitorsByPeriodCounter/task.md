Count how many visitors were during specific period of time
   
<div class="hint">
  Use <code>Flux#reduce(new HashMap(), ...)</code> or <code>Flux#collect(Collector)</code> or <code>Flux#collectMap()</code>
</div>